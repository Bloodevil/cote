class 509_FibonacciNum {
    public int fib(int n) {
      int [] arr = new int [31];

      arr[0] = 0;
      arr[1] = 1;

      if(n<2){
          return arr[n];
      }

      for(int i=2;i<n+1;i++){
          arr[i] = arr[i-2] + arr[i-1];
      }

      return arr[n];
    }
}
